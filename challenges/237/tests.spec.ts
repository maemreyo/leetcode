// import { describe, expect, test } from '@jest/globals'
// import { deleteNode, ListNode, sampleNodes } from './solution'

// describe('Delete Node in a Linked List', () => {

//     test("Should return the remaining list after deleting a node", () => {
//         expect(deleteNode(sampleNodes)).toBe(new ListNode(4, new ListNode(1, new ListNode(9))));
//     })

//     test("Return the original list if the target is a tail node", () => {
//         expect(deleteNode(sampleNodes)).toBe(sampleNodes);
//     })
// })